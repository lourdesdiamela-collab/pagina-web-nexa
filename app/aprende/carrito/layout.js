export const metadata = {
  title: 'Carrito | NEXA Aprende',
  description: 'Revisá los recursos que agregaste antes de finalizar tu compra en NEXA Aprende.',
  robots: { index: false, follow: true },
};

export default function CarritoLayout({ children }) {
  return children;
}
