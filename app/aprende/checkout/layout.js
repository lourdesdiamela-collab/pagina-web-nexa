export const metadata = {
  title: 'Checkout | NEXA Aprende',
  description: 'Finalizá tu compra de recursos descargables en NEXA Aprende.',
  robots: { index: false, follow: true },
};

export default function CheckoutLayout({ children }) {
  return children;
}
