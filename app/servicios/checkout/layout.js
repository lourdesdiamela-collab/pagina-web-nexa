export const metadata = {
  title: 'Checkout de Servicio | NEXA',
  description: 'Confirmá la contratación de tu servicio con NEXA.',
  robots: { index: false, follow: true },
};

export default function ServiciosCheckoutLayout({ children }) {
  return children;
}
